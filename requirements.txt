# DistCore Python dependencies
# Install: pip install -r requirements.txt
#
# System prerequisites (not installed via pip):
#   - ODBC Driver 17 for SQL Server
#   - Cisco AnyConnect (VPN automation on Windows only)

# --- Core ETL / DB ---
pyodbc>=5.0.0
PyYAML>=6.0
pandas>=2.0.0
openpyxl>=3.1.0
requests>=2.31.0

# DMS SharePoint auth
# Windows: integrated SSPI (optional when username/password are set)
requests-negotiate-sspi>=0.3.0; platform_system=="Windows"
# Linux / Docker (and Windows fallback): NTLM with username + password
requests-ntlm>=1.2.0

# --- Optimization ---
pulp>=2.7.0
scipy>=1.11.0
numpy>=1.24.0

# --- Visualization (scripts/visualize_optimization.py) ---
matplotlib>=3.7.0

# --- Web MVP (FastAPI) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9
httpx>=0.27.0
